<%= form_with(model: recipe, local: true) do |form| %>
  <% if recipe.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(recipe.errors.count, "error") %> prohibited this recipe from being saved:</h2>

      <ul>
      <% recipe.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :recipe_name %>
  </div>

  <div class="field">
    <%= form.label :Pizza_id %>
    <%= form.text_field :Pizza_id, id: :recipe_Pizza_id %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.number_field :price, id: :recipe_price %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
